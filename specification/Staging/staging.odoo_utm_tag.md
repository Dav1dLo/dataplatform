# odoo_utm_tag

## Source system
This table originates from Odoo, an open-source ERP and CRM platform. The presence of `create_uid`, `write_uid`, `create_date`, and `write_date` columns is characteristic of Odoo's standard ORM audit fields, and the `JSONB` type for the `name` field suggests a multi-language or localized configuration common in Odoo's metadata tables.

## Functional process 
This table supports the marketing tracking infrastructure, specifically managing UTM (Urchin Tracking Module) tags used to categorize and analyze the performance of marketing campaigns. It acts as a lookup or configuration table for the labels applied to incoming traffic sources.

## Description
One row in this table represents a single UTM tag definition used to categorize marketing traffic. It serves as a raw landed copy of the Odoo `utm.tag` model, capturing the configuration and audit metadata for each tag.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by Odoo. |
| color | INTEGER | true | UI color index | Used for visual grouping in the Odoo dashboard. |
| create_uid | INTEGER | true | Creator user ID | Foreign key to the Odoo user who created the tag. |
| write_uid | INTEGER | true | Last modifier user ID | Foreign key to the Odoo user who last updated the tag. |
| name | JSONB | false | Tag display name | Likely contains localized strings; handle as JSON. |
| create_date | TIMESTAMP | true | Creation timestamp | Recorded in UTC by the Odoo application. |
| write_date | TIMESTAMP | true | Last update timestamp | Recorded in UTC by the Odoo application. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `create_uid` → `res_users.id`: References the user who performed the initial creation.
    - `write_uid` → `res_users.id`: References the user who performed the last modification.
- **Natural keys (inferred):** 
    - `name`: While stored as JSONB, the business logic of Odoo typically enforces uniqueness on the tag name field.

## Caveats for downstream consumers

- **PII/Sensitivity:** None identified; this table contains configuration metadata for marketing tags.
- **Timestamps:** All timestamps (`create_date`, `write_date`) are assumed to be in UTC, consistent with Odoo's internal storage standards.
- **Data Format:** The `name` column is `JSONB`. Downstream queries will need to use the `->>` operator (e.g., `name->>'en_US'`) to extract specific string values.
- **Soft Deletes:** This table does not appear to have a `deleted` or `active` flag; assume all rows are active unless Odoo's standard `active` column (missing here) is added in future ingestion.