# odoo_website_visitor

## Source system
This table originates from Odoo ERP, specifically the website module. The naming convention (e.g., `website_id`, `partner_id`, `access_token`) and the presence of Odoo-standard audit columns (`create_uid`, `write_uid`, `create_date`, `write_date`) are characteristic of Odoo's internal ORM structure.

## Functional process 
This table supports the web analytics and visitor tracking process within the Odoo ecosystem. It tracks unique visitors to the website, linking them to internal partners where possible, and captures session-related metadata such as language, timezone, and visit frequency to support personalization and marketing attribution.

## Description
One row in this table represents a unique website visitor session or profile identified by an access token. This is a raw landed copy of the Odoo `website.visitor` model, serving as the base staging entity for downstream web traffic analysis and visitor engagement reporting.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by Odoo. |
| website_id | INTEGER | true | Foreign key to the website | Identifies which site the visitor accessed. |
| partner_id | INTEGER | true | Foreign key to the partner | Links the visitor to a known CRM contact/partner. |
| country_id | INTEGER | true | Foreign key to the country | Geolocation based on visitor IP. |
| lang_id | INTEGER | true | Foreign key to the language | Preferred language of the visitor. |
| visit_count | INTEGER | true | Total number of visits | Incremental counter for returning visitors. |
| create_uid | INTEGER | true | Creator user ID | Internal Odoo user ID who created the record. |
| write_uid | INTEGER | true | Last modifier user ID | Internal Odoo user ID who last updated the record. |
| access_token | VARCHAR | false | Unique visitor identifier | Used for tracking sessions across requests. |
| timezone | VARCHAR | true | Visitor timezone | String representation (e.g., 'UTC', 'Europe/Brussels'). |
| create_date | TIMESTAMP | true | Record creation timestamp | UTC timestamp of initial visitor registration. |
| last_connection_datetime | TIMESTAMP | true | Last activity timestamp | UTC timestamp of the most recent page view. |
| write_date | TIMESTAMP | true | Last update timestamp | UTC timestamp of the last record modification. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `website_id` → `website.id` (Inferred from Odoo naming convention).
    - `partner_id` → `res_partner.id` (Inferred from Odoo naming convention).
    - `country_id` → `res_country.id` (Inferred from Odoo naming convention).
    - `lang_id` → `res_lang.id` (Inferred from Odoo naming convention).
- **Natural keys (inferred):** 
    - `access_token` (Used by Odoo to uniquely identify the visitor session).

## Caveats for downstream consumers

- **Sensitive Data:** `access_token` should be treated as a session identifier and potentially masked if used in logs.
- **Timezones:** Timestamps are stored in UTC as per Odoo standard behavior.
- **Soft Deletes:** This table does not appear to implement a soft-delete flag (e.g., `active` column), which is common in other Odoo tables; assume all rows are active unless otherwise specified by business logic.
- **Data Quality:** `partner_id` will be null for anonymous visitors who have not logged in or filled out a form.