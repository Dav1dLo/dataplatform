# odoo_res_users_log

## Source system
This table originates from Odoo ERP. The naming convention `res_users_log` and the presence of standard Odoo audit columns (`create_uid`, `write_uid`, `create_date`, `write_date`) are characteristic of Odoo's internal resource management modules.

## Functional process 
This table supports the user activity and audit tracking process within the Odoo platform. It records metadata regarding the creation and modification of user-related logs, facilitating traceability of administrative actions performed on user accounts or system access logs.

## Description
One row in this table represents a single audit entry or log record associated with a user-related event in the Odoo system. It serves as a raw landed copy of the source system's audit trail, providing the necessary timestamps and user identifiers to track record lifecycle events.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by the source system. |
| create_uid | INTEGER | true | ID of the user who created the record | References the `res_users` table. |
| write_uid | INTEGER | true | ID of the user who last modified the record | References the `res_users` table. |
| create_date | TIMESTAMP | true | Timestamp of record creation | Assumed UTC; verify against Odoo server configuration. |
| write_date | TIMESTAMP | true | Timestamp of last modification | Assumed UTC; verify against Odoo server configuration. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `create_uid` → `res_users.id` (Standard Odoo pattern for tracking record creators).
    - `write_uid` → `res_users.id` (Standard Odoo pattern for tracking record modifiers).
- **Natural keys (inferred):** Not confidently inferable from the provided metadata.

## Caveats for downstream consumers

- **PII/Sensitive Data:** While this table contains only system IDs and timestamps, it is linked to user identities; ensure access controls are aligned with your organization's PII policies.
- **Timezones:** Timestamps are typically stored in UTC by Odoo, but confirm this against the specific instance's `ir.config_parameter` settings.
- **Soft Deletes:** This table does not appear to have a dedicated `active` or `deleted` flag; assume all records are active unless otherwise specified by the source system's business logic.
- **Data Integrity:** As a staging table, this may contain duplicates or partial records if the ingestion process is interrupted; consider deduplication logic based on `id` if necessary.