# odoo_account_journal_account_journal_group_rel

## Source system
This table originates from Odoo ERP. The naming convention `_rel` combined with the specific entity names `account_journal_group` and `account_journal` is characteristic of Odoo's automated many-to-many relationship tables generated by the ORM.

## Functional process 
This table supports the financial accounting module's configuration, specifically the grouping of journals. It facilitates the many-to-many relationship between journal groups and individual accounting journals, allowing a single journal to belong to multiple groups or a group to contain multiple journals for reporting and filtering purposes.

## Description
One row in this table represents a single association between an accounting journal group and an individual accounting journal. It serves as a raw junction table in the staging layer, capturing the link between these two entities as defined in the Odoo database schema.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| account_journal_group_id | INTEGER | false | Foreign key to the journal group definition | Links to the primary key of the journal group table. |
| account_journal_id | INTEGER | false | Foreign key to the accounting journal | Links to the primary key of the journal table. |

## Keys

- **Primary key (inferred):** The combination of `(account_journal_group_id, account_journal_id)` is the inferred composite primary key.
- **Foreign keys (inferred):** 
    - `account_journal_group_id → account_journal_group.id`: This column references the parent journal group entity.
    - `account_journal_id → account_journal.id`: This column references the specific journal entity.
- **Natural keys (inferred):** Not confidently inferable from the provided metadata; this is a pure junction table.

## Caveats for downstream consumers

- This table is a junction table; it contains no descriptive attributes, only identifiers.
- There are no timestamps or audit columns present; incremental loading logic should rely on full-table replacement or source-side change tracking if available.
- Ensure that joins to the parent tables handle the many-to-many cardinality correctly to avoid fan-out issues in downstream reporting.