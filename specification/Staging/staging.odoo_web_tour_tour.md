# odoo_web_tour_tour

## Source system
This table originates from Odoo, an open-source ERP and business management software. The naming convention `web_tour_tour` and the presence of standard Odoo audit columns (`create_uid`, `write_uid`, `create_date`, `write_date`) are characteristic of Odoo's internal module structure for managing interactive user onboarding tours.

## Functional process 
This table supports the "User Onboarding and Training" process within the Odoo platform. It stores the definitions and configurations for guided tours that assist users in navigating the interface, triggered by specific URLs or custom events.

## Description
One row in this table represents a single interactive tour definition available to users within the Odoo web interface. It acts as a raw landing copy of the tour configuration, capturing the tour's name, target URL, and associated success messages.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence generated by `web_tour_tour_id_seq`. |
| sequence | INTEGER | true | Display order index | Determines the order in which tours are presented. |
| create_uid | INTEGER | true | Creator user ID | Reference to the user who created the tour record. |
| write_uid | INTEGER | true | Last modifier user ID | Reference to the user who last updated the tour record. |
| name | VARCHAR | false | Tour name | The internal identifier or display name of the tour. |
| url | VARCHAR | true | Target URL | The specific path where the tour is triggered. |
| rainbow_man_message | JSONB | true | Success message payload | JSON content displayed upon tour completion. |
| custom | BOOLEAN | true | Custom flag | Indicates if the tour is a custom implementation. |
| create_date | TIMESTAMP | true | Creation timestamp | Timestamp of record creation (UTC). |
| write_date | TIMESTAMP | true | Last update timestamp | Timestamp of last modification (UTC). |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `create_uid` → `res_users.id` (Standard Odoo pattern for tracking record creators).
    - `write_uid` → `res_users.id` (Standard Odoo pattern for tracking record modifiers).
- **Natural keys (inferred):** 
    - `name` (Assuming tour names are unique within the Odoo instance).

## Caveats for downstream consumers

- **Sensitive Data:** No direct PII, but `rainbow_man_message` may contain arbitrary text content that should be audited for sensitive information.
- **Timestamps:** Timestamps are assumed to be in UTC, consistent with standard Odoo database configurations.
- **Soft Deletes:** This table does not appear to implement a soft-delete flag (e.g., `active` column); assume all rows are active unless otherwise specified by Odoo business logic.
- **JSONB:** The `rainbow_man_message` column requires `JSONB` handling in PostgreSQL; ensure downstream consumers are equipped to parse nested JSON structures.