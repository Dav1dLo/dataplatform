# odoo_stock_package_level

## Source system
This table originates from Odoo ERP, specifically the Inventory (stock) module. The naming convention `stock_package_level` and the presence of foreign keys like `picking_id` and `location_dest_id` are characteristic of Odoo's internal database schema for tracking package movements within warehouse operations.

## Functional process 
This table supports the warehouse management and logistics process, specifically tracking the association between physical packages and their movement through picking operations. It links specific inventory packages to picking orders and their destination locations, facilitating the tracking of consolidated shipments or palletized inventory.

## Description
One row in this table represents a single instance of a package being assigned to a specific picking operation or location movement. It serves as a raw landed copy of the Odoo `stock.package.level` model, capturing the state of package-level inventory movements at the time of ingestion.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by Odoo. |
| package_id | INTEGER | false | Foreign key to the package | References the physical package being moved. |
| picking_id | INTEGER | true | Foreign key to the picking | References the associated stock picking operation. |
| location_dest_id | INTEGER | true | Foreign key to the destination location | The target warehouse location for this package level. |
| company_id | INTEGER | false | Foreign key to the company | Identifies the organizational entity owning this record. |
| create_uid | INTEGER | true | Creator user ID | ID of the user who created the record. |
| write_uid | INTEGER | true | Last modifier user ID | ID of the user who last updated the record. |
| create_date | TIMESTAMP | true | Creation timestamp | Timestamp of record creation. |
| write_date | TIMESTAMP | true | Last update timestamp | Timestamp of the last record modification. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `package_id` → `staging.stock_quant_package.id` (Guess: links to the package definition).
    - `picking_id` → `staging.stock_picking.id` (Guess: links to the parent picking operation).
    - `location_dest_id` → `staging.stock_location.id` (Guess: links to the warehouse location hierarchy).
    - `company_id` → `staging.res_company.id` (Guess: links to the Odoo multi-company definition).
- **Natural keys (inferred):** Not confidently inferable from the provided metadata.

## Caveats for downstream consumers

- **Timestamps:** Timestamps are stored in the Odoo application timezone (typically UTC), but verify against the source system configuration.
- **Data Integrity:** `picking_id` and `location_dest_id` are nullable, implying that a package level might exist independently of a specific picking or destination during certain inventory states.
- **Soft Deletes:** Odoo typically uses hard deletes for this model; assume rows are removed from the source rather than flagged.
- **Sensitivity:** This table contains operational metadata; no direct PII is present, but it reflects internal warehouse movement patterns.