# odoo_sms_template_reset

## Source system
This table originates from Odoo ERP. The naming convention (`odoo_sms_template_reset`) and the presence of standard Odoo audit columns (`create_uid`, `write_uid`, `create_date`, `write_date`) are characteristic of Odoo's internal ORM-managed tables.

## Functional process 
This table supports the SMS marketing or notification module within Odoo, specifically tracking the reset or configuration state of SMS templates. It acts as an audit or state-tracking log for modifications made to SMS template settings by system users.

## Description
One row in this table represents a single record of an SMS template reset event or configuration entry. It serves as a raw landed copy of the Odoo database table, capturing the audit trail of who created or modified the record and when.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by Odoo. |
| create_uid | INTEGER | true | ID of the user who created the record | References `res.users`. |
| write_uid | INTEGER | true | ID of the user who last modified the record | References `res.users`. |
| create_date | TIMESTAMP | true | Timestamp of record creation | UTC assumed per Odoo standard. |
| write_date | TIMESTAMP | true | Timestamp of last modification | UTC assumed per Odoo standard. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `create_uid` → `res_users.id` (Standard Odoo audit pattern).
    - `write_uid` → `res_users.id` (Standard Odoo audit pattern).
- **Natural keys (inferred):** Not confidently inferable from the provided metadata.

## Caveats for downstream consumers

- **Sensitive Data:** Contains user IDs (`create_uid`, `write_uid`) which may need to be joined against a user dimension to identify specific personnel.
- **Timestamps:** Timestamps are assumed to be in UTC, consistent with standard Odoo database configurations.
- **Soft Deletes:** This table does not appear to have an `active` boolean column, which is common in Odoo for soft deletes; assume all rows are active unless otherwise specified by business logic.
- **Data Freshness:** As a staging table, this represents a raw snapshot; ensure downstream models handle potential duplicates or late-arriving data if the ingestion process is incremental.