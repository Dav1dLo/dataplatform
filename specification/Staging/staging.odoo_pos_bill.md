# odoo_pos_bill

## Source system
This table originates from Odoo ERP, specifically the Point of Sale (POS) module. The naming convention (`odoo_pos_bill`) and the presence of standard Odoo audit columns (`create_uid`, `write_uid`, `create_date`, `write_date`) are characteristic of Odoo's internal ORM-managed tables.

## Functional process 
This table supports the Point of Sale configuration and billing logic, likely managing specific bill or payment configuration rules within the POS interface. The `for_all_config` flag suggests this table defines global settings or specific bill templates that can be applied across different POS configurations.

## Description
One row in this table represents a single POS bill configuration record. It serves as a raw landing copy of the Odoo `pos.bill` model, capturing the configuration state, associated values, and audit metadata for each record.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by Odoo. |
| create_uid | INTEGER | true | User ID who created the record | References `res.users`. |
| write_uid | INTEGER | true | User ID who last updated the record | References `res.users`. |
| name | VARCHAR | true | Name or label of the bill configuration | Human-readable identifier. |
| value | NUMERIC | false | Monetary or numeric value associated with the bill | Precision not specified in source; verify scale. |
| for_all_config | BOOLEAN | true | Scope flag | If true, applies to all POS configurations. |
| create_date | TIMESTAMP | true | Record creation timestamp | UTC assumed per Odoo standards. |
| write_date | TIMESTAMP | true | Last update timestamp | UTC assumed per Odoo standards. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `create_uid` → `res_users.id` (Standard Odoo audit pattern).
    - `write_uid` → `res_users.id` (Standard Odoo audit pattern).
- **Natural keys (inferred):** Not confidently inferable from the provided metadata.

## Caveats for downstream consumers

- **Timestamps:** Timestamps are assumed to be in UTC, consistent with Odoo's internal storage.
- **Soft Deletes:** Odoo typically uses hard deletes for this type of configuration table; assume no soft-delete flag exists unless `active` is present (which is absent here).
- **Precision:** The `value` column uses `NUMERIC` without defined precision; verify if this represents currency (usually 2 decimal places) or a different unit before performing aggregations.
- **PII:** No direct PII is present, though `create_uid` and `write_uid` link to user identities.