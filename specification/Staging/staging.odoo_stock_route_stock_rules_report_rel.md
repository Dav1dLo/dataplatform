# odoo_stock_route_stock_rules_report_rel

## Source system
Odoo ERP. The table naming convention `_rel` combined with the specific module references (`stock_route`, `stock_rules_report`) is characteristic of Odoo's automated many-to-many relationship tables generated by the ORM.

## Functional process 
Inventory and supply chain management. This table supports the mapping between stock routes and stock rule reports, facilitating the configuration of complex logistics flows and replenishment rules within the Odoo warehouse management system.

## Description
One row represents a single association between a stock route and a stock rules report. This is a junction table used to resolve a many-to-many relationship between the two entities in the Odoo staging layer.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| stock_rules_report_id | INTEGER | false | Foreign key to the stock rules report entity | Links to the primary key of the report definition. |
| stock_route_id | INTEGER | false | Foreign key to the stock route entity | Links to the primary key of the stock route definition. |

## Keys

- **Primary key (inferred):** Composite key of (`stock_rules_report_id`, `stock_route_id`).
- **Foreign keys (inferred):** 
    - `stock_rules_report_id` → `staging.stock_rules_report.id`: This column references the parent report configuration.
    - `stock_route_id` → `staging.stock_route.id`: This column references the parent stock route configuration.
- **Natural keys (inferred):** Not confidently inferable from the provided metadata.

## Caveats for downstream consumers

- This is a pure junction table; it contains no descriptive attributes, only identifiers.
- Ensure inner joins are used when traversing these relationships to avoid orphaned records if referential integrity is not strictly enforced in the source Odoo instance.
- As a staging table, this represents a raw snapshot of the Odoo database schema; verify if the source system performs soft deletes, as this table may contain stale references if the parent entities were removed.