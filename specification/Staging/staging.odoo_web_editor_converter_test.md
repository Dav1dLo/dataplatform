# odoo_web_editor_converter_test

## Source system
This table originates from Odoo ERP. The naming convention `web_editor_converter_test` combined with standard Odoo audit columns (`create_uid`, `write_uid`, `create_date`, `write_date`) and Odoo-specific field types (e.g., `many2one`, `selection_str`) confirms this is a technical or testing table within the Odoo framework.

## Functional process 
This table supports internal system testing or validation of the Odoo web editor's data conversion logic. It serves as a sandbox or schema-validation entity to ensure that various Odoo field types (HTML, binary, numeric, etc.) are correctly serialized and persisted through the web interface's conversion layer.

## Description
One row represents a single test record containing a variety of Odoo field types, ranging from simple integers and strings to complex HTML blobs and binary data. As a staging table, it acts as a raw, direct copy of the Odoo database record, intended for downstream validation or integration testing.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by Odoo. |
| integer | INTEGER | true | Integer field value | Standard integer storage. |
| many2one | INTEGER | true | Foreign key reference | Likely points to another Odoo model ID. |
| create_uid | INTEGER | true | Creator user ID | References the user who created the record. |
| write_uid | INTEGER | true | Last modifier user ID | References the user who last updated the record. |
| char | VARCHAR | true | Character string | Short text field. |
| selection_str | VARCHAR | true | Selection field value | Stores the key of an Odoo selection field. |
| date | DATE | true | Date value | Calendar date. |
| html | TEXT | true | HTML content | Stores formatted text/markup. |
| text | TEXT | true | Long text content | Unformatted long-form text. |
| numeric | NUMERIC | true | Fixed-point number | Used for precise decimal values. |
| datetime | TIMESTAMP | true | Datetime value | Timestamp without timezone. |
| create_date | TIMESTAMP | true | Record creation timestamp | Audit timestamp. |
| write_date | TIMESTAMP | true | Record modification timestamp | Audit timestamp. |
| float | DOUBLE PRECISION | true | Floating point number | Used for approximate decimal values. |
| binary | BYTEA | true | Binary data | Stores file blobs or encoded binary data. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `many2one` → `unknown_table.id` (Guess: standard Odoo naming convention for relational fields).
    - `create_uid` → `res_users.id` (Guess: standard Odoo audit column).
    - `write_uid` → `res_users.id` (Guess: standard Odoo audit column).
- **Natural keys (inferred):** Not confidently inferable from the provided metadata.

## Caveats for downstream consumers

- **Sensitive Data:** The `binary` column may contain sensitive file attachments or images; ensure appropriate handling if PII is stored in attachments.
- **Timezones:** Timestamps (`datetime`, `create_date`, `write_date`) are stored as `TIMESTAMP` (without timezone). Assume UTC unless otherwise specified by the Odoo instance configuration.
- **Data Integrity:** As this is a "test" table, expect inconsistent data patterns or sparse population of columns compared to production business tables.
- **Soft Deletes:** Odoo typically does not use soft-delete flags in these types of tables; assume rows are physically deleted if they disappear from the source.