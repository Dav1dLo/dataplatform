# odoo_pos_note

## Source system
This table originates from Odoo ERP, specifically the Point of Sale (POS) module. The naming convention (`odoo_pos_note`) and the presence of standard Odoo audit columns (`create_uid`, `write_uid`, `create_date`, `write_date`) are characteristic of Odoo's internal ORM-managed tables.

## Functional process 
This table supports the Point of Sale configuration and transaction process, specifically managing custom notes or labels that can be attached to POS orders or order lines. It acts as a lookup or configuration table for predefined notes used by cashiers during the checkout process.

## Description
One row in this table represents a single POS note definition available for selection within the Odoo Point of Sale interface. This is a raw landed staging table, representing a direct copy of the Odoo `pos_note` table, intended to provide a base for downstream dimension modeling.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by Odoo. |
| sequence | INTEGER | true | Display order | Determines the sort order in the POS UI. |
| create_uid | INTEGER | true | Creator user ID | Foreign key to the Odoo `res_users` table. |
| write_uid | INTEGER | true | Last modifier user ID | Foreign key to the Odoo `res_users` table. |
| name | VARCHAR | false | Note text | The actual content of the note displayed to the user. |
| create_date | TIMESTAMP | true | Creation timestamp | Recorded in UTC by the Odoo application. |
| write_date | TIMESTAMP | true | Last update timestamp | Recorded in UTC by the Odoo application. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `create_uid` → `res_users.id` (Standard Odoo audit pattern for record creation).
    - `write_uid` → `res_users.id` (Standard Odoo audit pattern for record modification).
- **Natural keys (inferred):** Not confidently inferable. While `name` might be unique in practice, Odoo typically relies on the surrogate `id` for internal references.

## Caveats for downstream consumers

- **Sensitive Data:** Contains no PII or payment data; safe for general access.
- **Timezones:** Timestamps (`create_date`, `write_date`) are stored in UTC by Odoo.
- **Soft Deletes:** This table does not appear to implement a soft-delete flag (e.g., `active` column), which is common in other Odoo tables. Assume all rows are active unless otherwise specified by business logic.
- **Data Integrity:** As a staging table, it may contain orphaned records if the source system's referential integrity is not strictly enforced at the database level.