# odoo_sms_account_sender

## Source system
This table originates from Odoo ERP, as indicated by the naming convention `odoo_sms_account_sender` and the presence of standard Odoo audit columns such as `create_uid`, `write_uid`, `create_date`, and `write_date`.

## Functional process 
This table supports the SMS communication module within Odoo, specifically managing the configuration of sender identities (e.g., short codes or alphanumeric sender IDs) associated with specific Odoo accounts or organizations. It acts as a registry for mapping sender aliases to account-level SMS settings.

## Description
One row in this table represents a single SMS sender configuration linked to an Odoo account. This is a raw landing table in the Staging layer, providing a direct, un-transformed snapshot of the source system's sender configuration records.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by Odoo. |
| account_id | INTEGER | false | Foreign key to the associated account | Links the sender to a specific Odoo account entity. |
| create_uid | INTEGER | true | User ID who created the record | References the Odoo user table. |
| write_uid | INTEGER | true | User ID who last updated the record | References the Odoo user table. |
| sender_name | VARCHAR | true | Alphanumeric sender ID or short code | The identity displayed to SMS recipients. |
| create_date | TIMESTAMP | true | Record creation timestamp | Inferred UTC based on Odoo standard behavior. |
| write_date | TIMESTAMP | true | Last modification timestamp | Inferred UTC based on Odoo standard behavior. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `account_id → odoo_account.id` (Guess: standard Odoo pattern for linking configuration to parent accounts).
    - `create_uid → odoo_res_users.id` (Guess: standard Odoo audit column).
    - `write_uid → odoo_res_users.id` (Guess: standard Odoo audit column).
- **Natural keys (inferred):** Not confidently inferable from the provided metadata.

## Caveats for downstream consumers

- **Timestamps:** Assumed to be in UTC, consistent with Odoo's internal storage format.
- **Soft Deletes:** Odoo typically uses `active` boolean flags for soft deletes; since no such column exists here, assume this table contains all records currently present in the source database.
- **Data Quality:** `sender_name` is nullable; ensure downstream logic handles missing sender identities gracefully.
- **Sensitivity:** No direct PII is present, but `account_id` and user IDs should be handled according to internal data governance policies.