# odoo_res_country_group

## Source system
This table originates from Odoo ERP, as indicated by the `res_country_group` naming convention, which is the standard technical name for the country group model in the Odoo framework. The presence of `create_uid`, `write_uid`, and `JSONB` fields for multi-language support is characteristic of Odoo's PostgreSQL-based backend.

## Functional process 
This table supports the management of geographical groupings of countries, often used in Odoo for defining pricing rules, tax configurations, or shipping zones that apply to specific sets of countries. It acts as a reference data entity within the broader sales and localization configuration modules.

## Description
One row in this table represents a single country group definition within the Odoo system. This is a raw landed copy of the Odoo `res.country.group` model, capturing the metadata and localized names for these groups. The grain is one row per unique country group identifier.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by Odoo. |
| create_uid | INTEGER | true | User ID who created the record | References `res.users`. |
| write_uid | INTEGER | true | User ID who last modified the record | References `res.users`. |
| name | JSONB | false | Name of the country group | Stores multi-language strings; requires extraction for reporting. |
| create_date | TIMESTAMP | true | Record creation timestamp | Inferred UTC based on Odoo standard. |
| write_date | TIMESTAMP | true | Last modification timestamp | Inferred UTC based on Odoo standard. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `create_uid` → `res_users.id` (Standard Odoo audit field referencing the user table).
    - `write_uid` → `res_users.id` (Standard Odoo audit field referencing the user table).
- **Natural keys (inferred):** Not confidently inferable from the provided metadata.

## Caveats for downstream consumers

- The `name` column is a `JSONB` object; query writers must use the `->>` operator (e.g., `name->>'en_US'`) to extract specific language values.
- Timestamps are assumed to be in UTC, consistent with Odoo's internal storage practices.
- This table contains audit fields (`create_uid`, `write_uid`) that link to the Odoo user system; ensure join logic accounts for potential missing user records.
- No soft-delete flag is present; Odoo typically performs hard deletes on this model unless specific modules are installed.