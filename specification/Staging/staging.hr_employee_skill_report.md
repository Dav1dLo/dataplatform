# hr_employee_skill_report

## Source system
The table likely originates from an internal Human Resources Information System (HRIS) or a Talent Management platform. The presence of granular identifiers such as `employee_id`, `department_id`, and `skill_type_id` suggests a system designed to track workforce competencies and professional development metrics.

## Functional process 
This table supports the Skills Management and Employee Development process. It tracks the proficiency levels and progress of employees across various technical or soft skills, likely used to generate competency reports, identify skill gaps within departments, or track progress toward internal certification milestones.

## Description
One row in this table represents a single skill record associated with a specific employee, capturing their current proficiency level and progress status. As a staging table, it serves as a raw, landed copy of the source HR system's skill assessment data, intended for subsequent transformation into analytical dimensions or fact tables.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | BIGINT | true | Surrogate primary key | Likely generated by the source system. |
| employee_id | INTEGER | true | Foreign key to the employee | Identifies the individual being assessed. |
| company_id | INTEGER | true | Foreign key to the company | Identifies the organization or business unit. |
| department_id | INTEGER | true | Foreign key to the department | Identifies the department the employee belongs to. |
| skill_id | INTEGER | true | Foreign key to the skill definition | Identifies the specific skill being measured. |
| skill_type_id | INTEGER | true | Foreign key to the skill category | Identifies the classification of the skill. |
| level_progress | NUMERIC | true | Quantitative progress metric | Represents the percentage or score of skill completion. |
| skill_level | VARCHAR | true | Qualitative proficiency label | e.g., "Beginner", "Intermediate", "Expert". |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `employee_id` → `dim_employee.id` (Guess: standard HRIS relationship)
    - `company_id` → `dim_company.id` (Guess: standard multi-tenant or organizational structure)
    - `department_id` → `dim_department.id` (Guess: standard organizational hierarchy)
    - `skill_id` → `dim_skill.id` (Guess: standard reference table for skills)
- **Natural keys (inferred):** Not confidently inferable from the provided metadata.

## Caveats for downstream consumers

- The `id` column is nullable, which is unusual for a primary key; verify if this indicates missing records or data quality issues in the source.
- All columns are nullable; ensure null-handling logic (e.g., `COALESCE`) is implemented in downstream transformations.
- The `skill_level` column is a `VARCHAR` without defined length; downstream systems should be prepared for varying string lengths.
- This table contains no audit timestamps (e.g., `created_at`, `updated_at`), making it difficult to perform incremental loads or track historical changes without additional metadata.