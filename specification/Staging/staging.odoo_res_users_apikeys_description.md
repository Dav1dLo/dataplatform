# odoo_res_users_apikeys_description

## Source system
This table originates from Odoo ERP, specifically the `res.users.apikeys.description` model. The naming convention `res_users` is a standard Odoo pattern for core user management and authentication modules.

## Functional process 
This table supports the API key management process for user authentication. It tracks the metadata and lifecycle of API keys generated by users to access Odoo services programmatically, ensuring that keys are associated with specific users and have defined expiration parameters.

## Description
One row represents a single API key description record associated with a user account. It serves as a raw landed copy of the Odoo system's internal tracking for API key metadata, capturing creation, modification, and expiration details.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by Odoo. |
| create_uid | INTEGER | true | User ID who created the record | References `res.users`. |
| write_uid | INTEGER | true | User ID who last modified the record | References `res.users`. |
| name | VARCHAR | false | Display name of the API key | Usually a user-defined label. |
| duration | VARCHAR | false | Validity duration string | Likely stores interval definitions. |
| expiration_date | TIMESTAMP | true | Expiration timestamp | UTC assumed; null if no expiry. |
| create_date | TIMESTAMP | true | Record creation timestamp | UTC assumed. |
| write_date | TIMESTAMP | true | Last modification timestamp | UTC assumed. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `create_uid` → `res_users.id` (Inferred from Odoo standard `_uid` naming convention).
    - `write_uid` → `res_users.id` (Inferred from Odoo standard `_uid` naming convention).
- **Natural keys (inferred):** Not confidently inferable from the provided metadata.

## Caveats for downstream consumers

- **Timestamps:** Assumed to be in UTC, consistent with standard Odoo database configurations.
- **Data Sensitivity:** Contains metadata about API keys; while the keys themselves are not stored here, the existence of these records confirms user access patterns.
- **Soft Deletes:** Odoo typically uses `active` flags for soft deletes; since no `active` column is present, this table may represent only active records or require checking for a missing column.
- **Precision:** `VARCHAR` lengths are not explicitly defined in the source metadata; downstream consumers should handle variable-length strings defensively.