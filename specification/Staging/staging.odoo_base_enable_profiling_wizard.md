# odoo_base_enable_profiling_wizard

## Source system
This table originates from Odoo ERP. The naming convention `base_enable_profiling_wizard` and the presence of standard Odoo audit columns (`create_uid`, `write_uid`, `create_date`, `write_date`) are characteristic of Odoo's internal wizard models used for administrative configuration and system profiling.

## Functional process 
This table supports the system administration and performance monitoring process within Odoo. It tracks the configuration of profiling sessions, specifically managing the duration and expiration of performance profiling tasks initiated by administrative users.

## Description
One row in this table represents a single instance of a profiling wizard configuration, which defines the parameters for a system performance trace. As a staging table, it serves as a raw, landed copy of the Odoo database record, capturing the state of profiling requests at the time of ingestion.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by Odoo. |
| create_uid | INTEGER | true | User ID who created the record | References `res_users.id`. |
| write_uid | INTEGER | true | User ID who last updated the record | References `res_users.id`. |
| duration | VARCHAR | true | Duration of the profiling session | Likely stores a string representation of time intervals. |
| expiration | TIMESTAMP | true | Expiration timestamp of the profile | Assumed UTC; indicates when the trace stops. |
| create_date | TIMESTAMP | true | Record creation timestamp | Assumed UTC. |
| write_date | TIMESTAMP | true | Last update timestamp | Assumed UTC. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `create_uid` → `res_users.id` (Standard Odoo audit pattern for creator).
    - `write_uid` → `res_users.id` (Standard Odoo audit pattern for modifier).
- **Natural keys (inferred):** Not confidently inferable from the provided metadata.

## Caveats for downstream consumers

- **Sensitive Data:** Contains user IDs (`create_uid`, `write_uid`) which may need to be joined with user dimension tables; no direct PII like names or emails is present.
- **Timezones:** All `TIMESTAMP` columns are assumed to be in UTC, consistent with standard Odoo database storage practices.
- **Data Retention:** This table represents a raw landing; it does not explicitly indicate if records are purged after the profiling session expires.
- **Type Precision:** The `duration` column is typed as `VARCHAR` in the source; downstream consumers should implement casting logic if numerical analysis of duration is required.