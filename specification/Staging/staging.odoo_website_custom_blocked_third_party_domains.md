# odoo_website_custom_blocked_third_party_domains

## Source system
This table originates from Odoo ERP, specifically the website module. The naming convention `odoo_website_...` and the presence of standard Odoo audit columns (`create_uid`, `write_uid`, `create_date`, `write_date`) are characteristic of Odoo's internal ORM-managed tables.

## Functional process 
This table supports website security and content management by maintaining a blacklist of third-party domains. It is likely used to restrict external scripts, trackers, or API calls from being executed or loaded on the Odoo-hosted website, serving as a configuration store for web security policies.

## Description
Each row represents a single domain or content string that has been explicitly blocked from interacting with the website. This is a raw landing table in the staging layer, containing a direct copy of the Odoo database record used for domain filtering.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by Odoo. |
| create_uid | INTEGER | true | User ID who created the record | References the Odoo `res.users` table. |
| write_uid | INTEGER | true | User ID who last updated the record | References the Odoo `res.users` table. |
| content | TEXT | true | The blocked domain or pattern | The actual string value being blacklisted. |
| create_date | TIMESTAMP | true | Record creation timestamp | Odoo internal timestamp, usually UTC. |
| write_date | TIMESTAMP | true | Last update timestamp | Odoo internal timestamp, usually UTC. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `create_uid` → `res_users.id` (guess: standard Odoo audit pattern).
    - `write_uid` → `res_users.id` (guess: standard Odoo audit pattern).
- **Natural keys (inferred):** `content` (assuming the domain/pattern string is intended to be unique).

## Caveats for downstream consumers

- **Sensitive Data:** This table contains configuration data; while not PII, it defines security boundaries for the website.
- **Timestamps:** Timestamps are stored in Odoo's default format, which is typically UTC.
- **Soft Deletes:** This table does not appear to have an `active` boolean flag, which is common in Odoo; verify if records are physically deleted or if this table is append-only.
- **Data Quality:** The `content` column is `TEXT` and may contain varied patterns (e.g., full URLs, domain wildcards, or regex); downstream consumers should implement robust parsing.