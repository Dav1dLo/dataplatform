# odoo_stock_quant_stock_track_confirmation_rel

## Source system
This table originates from Odoo ERP. The naming convention `_rel` combined with the specific module-related prefixes `stock_track_confirmation` and `stock_quant` is characteristic of Odoo's automated many-to-many relationship tables generated by the ORM.

## Functional process 
This table supports the inventory tracking and traceability process. It maps specific inventory quant records (representing stock at a specific location) to stock tracking confirmation events, likely used to verify serial or lot number movements within the warehouse management system.

## Description
This table represents a many-to-many join relationship between stock tracking confirmations and stock quants. Each row represents a single link between a confirmation event and an inventory quantity record. As a staging table, it serves as a raw, normalized link entity to be joined into downstream fact tables for inventory auditing.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| stock_track_confirmation_id | INTEGER | false | Foreign key to the stock tracking confirmation record. | Links to the parent confirmation event. |
| stock_quant_id | INTEGER | false | Foreign key to the stock quant record. | Links to the specific inventory quantity/location record. |

## Keys

- **Primary key (inferred):** Not confidently inferable. This table likely relies on a composite primary key of `(stock_track_confirmation_id, stock_quant_id)`.
- **Foreign keys (inferred):** 
    - `stock_track_confirmation_id` → `stock_track_confirmation.id`: This column references the primary key of the confirmation event table.
    - `stock_quant_id` → `stock_quant.id`: This column references the primary key of the inventory quant table.
- **Natural keys (inferred):** The combination of `(stock_track_confirmation_id, stock_quant_id)` acts as the unique business identifier for this relationship.

## Caveats for downstream consumers

- This table contains no timestamps or audit metadata; it is a pure join table.
- There are no sensitive PII columns in this table.
- As a join table in Odoo, this record is typically deleted if the relationship is removed in the source system; it does not support soft deletes.
- Ensure that joins to this table are handled as a bridge to avoid fan-out issues when aggregating quantities from `stock_quant`.