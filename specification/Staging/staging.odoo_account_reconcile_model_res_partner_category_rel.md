# odoo_account_reconcile_model_res_partner_category_rel

## Source system
This table originates from Odoo ERP. The naming convention `_rel` combined with the specific module prefixes `account_reconcile_model` and `res_partner_category` is characteristic of Odoo's many-to-many relationship tables, which are automatically generated by the ORM to link reconciliation models to partner categories.

## Functional process 
This table supports the automated bank reconciliation process. It defines the many-to-many relationship between accounting reconciliation models and partner categories, allowing the system to apply specific reconciliation rules (such as tax defaults or account mappings) based on the category assigned to a business partner.

## Description
One row in this table represents a single association between a specific reconciliation model and a partner category. It serves as a raw landing copy of the Odoo join table, facilitating the mapping of business logic between accounting automation and partner classification.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| account_reconcile_model_id | INTEGER | false | Foreign key to the reconciliation model | Links to the primary key of the reconciliation model definition. |
| res_partner_category_id | INTEGER | false | Foreign key to the partner category | Links to the primary key of the partner category definition. |

## Keys

- **Primary key (inferred):** The composite key of (`account_reconcile_model_id`, `res_partner_category_id`).
- **Foreign keys (inferred):** 
    - `account_reconcile_model_id` → `account_reconcile_model.id`: This column references the definition of the reconciliation rule.
    - `res_partner_category_id` → `res_partner_category.id`: This column references the classification tag assigned to partners.
- **Natural keys (inferred):** Not confidently inferable from the provided metadata.

## Caveats for downstream consumers

- This table is a pure join table; it contains no descriptive attributes, only identifiers.
- There are no timestamps or audit columns present in this table; it reflects the current state of the relationship as defined in the Odoo database.
- Ensure that joins to this table are performed on both columns to maintain the integrity of the many-to-many relationship.