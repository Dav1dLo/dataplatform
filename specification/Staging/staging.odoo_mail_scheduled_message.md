# odoo_mail_scheduled_message

## Source system
This table originates from Odoo ERP, a modular business management software. The naming convention (`res_id`, `create_uid`, `write_uid`, `model`) and the specific structure of the mail messaging system are characteristic of Odoo's internal ORM-based database schema.

## Functional process 
This table supports the communication and notification engine within Odoo, specifically managing the queue for delayed or scheduled outgoing messages. It tracks messages linked to specific business objects (e.g., invoices, sales orders) that are pending delivery or internal notification.

## Description
Each row represents a single scheduled message or notification record intended for a specific business entity. This is a raw landing table in the staging layer, capturing the state of the Odoo `mail.scheduled.message` model as it exists in the source database.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by Odoo. |
| res_id | INTEGER | false | Related record ID | The ID of the business object the message is attached to. |
| author_id | INTEGER | false | Author ID | The ID of the user or partner who authored the message. |
| create_uid | INTEGER | true | Creator user ID | The ID of the user who created the record. |
| write_uid | INTEGER | true | Last updater user ID | The ID of the user who last modified the record. |
| subject | VARCHAR | true | Message subject | The subject line of the message. |
| model | VARCHAR | false | Related model name | The technical name of the Odoo model (e.g., 'sale.order'). |
| body | TEXT | true | Message content | The HTML or plain text body of the message. |
| notification_parameters | TEXT | true | Notification config | JSON or serialized parameters for the notification engine. |
| is_note | BOOLEAN | true | Note flag | Indicates if the message is an internal note rather than an email. |
| scheduled_date | TIMESTAMP | false | Execution timestamp | The date and time the message is scheduled to be processed. |
| create_date | TIMESTAMP | true | Creation timestamp | The date and time the record was created. |
| write_date | TIMESTAMP | true | Modification timestamp | The date and time the record was last updated. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `author_id` → `res_partner.id` (guess: standard Odoo pattern for message authors).
    - `create_uid` → `res_users.id` (guess: standard Odoo audit field).
    - `write_uid` → `res_users.id` (guess: standard Odoo audit field).
- **Natural keys (inferred):** Not confidently inferable.

## Caveats for downstream consumers

- **Timestamps:** Timestamps are typically stored in UTC in Odoo; verify against the source system configuration.
- **Sensitive Data:** The `body` column may contain PII or sensitive business communication; ensure appropriate masking if exposing to non-privileged users.
- **Soft Deletes:** This table does not appear to implement a soft-delete flag; assume rows are physically deleted if removed from the source.
- **Model Polymorphism:** The `res_id` column is polymorphic; it must be joined against the table specified in the `model` column to resolve the actual business entity.