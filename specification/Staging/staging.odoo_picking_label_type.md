# odoo_picking_label_type

## Source system
This table originates from Odoo ERP. The naming convention (e.g., `create_uid`, `write_date`, `id` sequence) is characteristic of Odoo's ORM-generated database schema, which tracks record creation and modification metadata for all entities.

## Functional process 
This table supports the inventory and logistics management process, specifically defining the classification or format of labels generated for picking operations (e.g., shipping labels, pallet tags, or bin labels). It acts as a lookup or configuration table used during the warehouse fulfillment workflow.

## Description
One row in this table represents a specific type of label configuration available for use within the Odoo picking system. As a staging table, it provides a raw, direct copy of the Odoo configuration entity, serving as the foundation for downstream dimension tables that categorize warehouse documentation.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by Odoo. |
| create_uid | INTEGER | true | User ID who created the record | References the Odoo `res_users` table. |
| write_uid | INTEGER | true | User ID who last modified the record | References the Odoo `res_users` table. |
| label_type | VARCHAR | false | Descriptive name of the label type | The business-facing identifier for the label format. |
| create_date | TIMESTAMP | true | Record creation timestamp | Assumed UTC; Odoo standard. |
| write_date | TIMESTAMP | true | Last modification timestamp | Assumed UTC; Odoo standard. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `create_uid` → `res_users.id` (Inferred from Odoo standard naming for audit fields).
    - `write_uid` → `res_users.id` (Inferred from Odoo standard naming for audit fields).
- **Natural keys (inferred):** 
    - `label_type` (Assuming unique label type names within the Odoo instance).

## Caveats for downstream consumers

- **Sensitive Data:** Contains user IDs (`create_uid`, `write_uid`) which may link to employee names in other tables; ensure appropriate access controls.
- **Timestamps:** Odoo typically stores timestamps in UTC. Verify against the source instance configuration if local time conversion is required.
- **Soft Deletes:** This table does not appear to have a `deleted` or `active` flag; however, Odoo often uses an `active` boolean column for soft deletes. If missing, assume all records are current.
- **Data Precision:** `label_type` length is not explicitly defined in the source metadata; assume standard string handling.