# odoo_stock_inventory_warning

## Source system
This table originates from Odoo ERP, as indicated by the naming convention `odoo_stock_inventory_warning` and the presence of standard Odoo audit columns (`create_uid`, `write_uid`, `create_date`, `write_date`).

## Functional process 
This table supports the inventory management process, specifically tracking warnings or alerts generated during stock inventory adjustments or reconciliation cycles. It acts as a log or notification registry for inventory discrepancies identified within the Odoo warehouse module.

## Description
One row in this table represents a single inventory warning event or alert record generated by the Odoo system. This is a raw landed copy of the Odoo `stock.inventory.warning` model, serving as the staging point for downstream inventory audit and reconciliation reporting.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Uses sequence `staging.stock_inventory_warning_id_seq`. |
| create_uid | INTEGER | true | User ID who created the record | References the Odoo `res.users` table. |
| write_uid | INTEGER | true | User ID who last updated the record | References the Odoo `res.users` table. |
| create_date | TIMESTAMP | true | Record creation timestamp | Assumed UTC; Odoo standard format. |
| write_date | TIMESTAMP | true | Last update timestamp | Assumed UTC; Odoo standard format. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `create_uid` → `res_users.id` (Standard Odoo pattern for record ownership).
    - `write_uid` → `res_users.id` (Standard Odoo pattern for record modification).
- **Natural keys (inferred):** Not confidently inferable from the provided metadata.

## Caveats for downstream consumers

- **Timestamps:** All `_date` columns are assumed to be in UTC, consistent with standard Odoo database configurations.
- **PII:** This table contains system audit IDs but no direct PII; however, `create_uid` and `write_uid` link to user identity tables which may contain sensitive information.
- **Data Retention:** This is a staging table; it reflects the state of the source system at the time of ingestion. It does not explicitly implement soft-delete flags, so downstream logic should handle potential duplicates if the source system performs hard deletes.