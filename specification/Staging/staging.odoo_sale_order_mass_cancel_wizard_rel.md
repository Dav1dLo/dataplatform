# odoo_sale_order_mass_cancel_wizard_rel

## Source system
This table originates from Odoo ERP. The naming convention `*_rel` combined with the specific wizard-related prefix suggests this is a join table generated by the Odoo ORM to manage a many-to-many relationship between a mass-cancellation wizard session and the specific sales orders selected for cancellation.

## Functional process 
This table supports the "Order Management" or "Sales Operations" process. It acts as a temporary or transactional link that tracks which sales orders are currently queued for a bulk cancellation action initiated via the Odoo user interface.

## Description
One row in this table represents a single association between a specific mass-cancellation wizard instance and a sales order. It is a raw landed copy of an Odoo relational join table, serving as the bridge to resolve the many-to-many relationship between the wizard session and the target sales orders.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| sale_mass_cancel_orders_id | INTEGER | false | Foreign key to the mass cancel wizard session. | Represents the ID of the wizard instance. |
| sale_order_id | INTEGER | false | Foreign key to the sales order being cancelled. | Links to the primary sales order record. |

## Keys

- **Primary key (inferred):** The combination of `(sale_mass_cancel_orders_id, sale_order_id)` is the inferred composite primary key.
- **Foreign keys (inferred):** 
    - `sale_mass_cancel_orders_id` → `sale_mass_cancel_orders.id` (Guess: links to the parent wizard session record).
    - `sale_order_id` → `sale_order.id` (Guess: links to the specific sales order record).
- **Natural keys (inferred):** Not confidently inferable from the provided metadata.

## Caveats for downstream consumers

- This table represents a transient or operational relationship; ensure that downstream models account for the fact that these records may be purged or short-lived in the source system after the wizard process completes.
- There are no timestamps or audit columns; it is impossible to determine the age of these associations without joining to the parent wizard table.
- This is a pure join table; it contains no business data other than the identifiers for the two entities it connects.