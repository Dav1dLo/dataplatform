# odoo_hr_employee_category

## Source system
This table originates from Odoo ERP, specifically the Human Resources module. The naming convention `hr_employee_category` and the presence of standard Odoo audit columns (`create_uid`, `write_uid`, `create_date`, `write_date`) are characteristic of Odoo's internal ORM structure.

## Functional process 
This table supports the employee management and organizational structure process. It defines categories or tags used to classify employees (e.g., "Full-time", "Contractor", "Management"), which are likely used for filtering, reporting, or access control within the HR module.

## Description
One row represents a single employee category definition within the Odoo HR system. This is a raw landing table in the Staging layer, providing a direct, un-transformed copy of the source database table used to categorize the workforce.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by Odoo. |
| color | INTEGER | true | UI color index | Represents the color assigned to the category in the Odoo web interface. |
| create_uid | INTEGER | true | Creator user ID | Foreign key to the user who created this record. |
| write_uid | INTEGER | true | Last modifier user ID | Foreign key to the user who last updated this record. |
| name | VARCHAR | false | Category name | The human-readable label for the employee category. |
| create_date | TIMESTAMP | true | Creation timestamp | Timestamp when the record was created. |
| write_date | TIMESTAMP | true | Last update timestamp | Timestamp when the record was last modified. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `create_uid` → `res_users.id` (Guess: standard Odoo audit column referencing the users table).
    - `write_uid` → `res_users.id` (Guess: standard Odoo audit column referencing the users table).
- **Natural keys (inferred):** 
    - `name` (Assuming category names are unique within the Odoo instance).

## Caveats for downstream consumers

- **Sensitive Data:** This table contains no PII, but `create_uid` and `write_uid` link to internal system users.
- **Timestamps:** Timestamps are stored in the Odoo application server time (typically UTC), but verify against the source system configuration.
- **Soft Deletes:** Odoo typically uses hard deletes for configuration tables; however, verify if records are removed or if an `active` flag (not present here) is missing.
- **Data Precision:** The `VARCHAR` type for `name` does not specify a length; assume standard Odoo defaults (often 255 characters) but check source DDL if truncation occurs.