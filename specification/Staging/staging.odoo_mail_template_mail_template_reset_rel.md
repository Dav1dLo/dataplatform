# odoo_mail_template_mail_template_reset_rel

## Source system
This table originates from Odoo ERP. The naming convention `_rel` is characteristic of Odoo's automated many-to-many relationship tables, which are generated by the ORM to link two distinct business entities.

## Functional process 
This table supports the communication and notification management module within Odoo. It facilitates the many-to-many relationship between email templates and specific reset or configuration triggers, ensuring that the correct templates are associated with system-generated reset events.

## Description
One row in this table represents a single association between a mail template and a reset configuration record. It serves as a raw junction table in the staging layer, capturing the link between these two entities as they exist in the source database.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| mail_template_reset_id | INTEGER | false | Foreign key to the reset configuration entity. | Maps to the primary key of the related reset table. |
| mail_template_id | INTEGER | false | Foreign key to the mail template entity. | Maps to the primary key of the mail template table. |

## Keys

- **Primary key (inferred):** The combination of `mail_template_reset_id` and `mail_template_id` forms the composite primary key.
- **Foreign keys (inferred):** 
    - `mail_template_reset_id` → `mail_template_reset.id` (Inferred from Odoo naming convention).
    - `mail_template_id` → `mail_template.id` (Inferred from Odoo naming convention).
- **Natural keys (inferred):** Not confidently inferable from the provided metadata.

## Caveats for downstream consumers

- This is a junction table; it contains no descriptive attributes, only identifiers.
- There are no timestamps or audit columns present in this table; it reflects the current state of the relationship as landed from the source.
- Ensure that joins to the target tables handle potential orphans if the source system's referential integrity is not strictly enforced at the database level.